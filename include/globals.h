/*
 * globals.h
 *
 * Constants and defines that would be useful to any part of the program should go here.
 *
 * A bunch of standard C header files are also included here to make life easier
 * (just include global.h instead of all the libraries individually)
 *
 *  Created on: May 11, 2010
 */

#ifndef GLOBALS_H_
#define GLOBALS_H_

#define MAX(X,Y) ((X > Y) ? X:Y)
#define MIN(X,Y) ((X < Y) ? X:Y)

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>
#include <sys/time.h>
#include <limits.h>

#endif /* GLOBALS_H_ */
